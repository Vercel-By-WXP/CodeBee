# 2026-09-21 竞品调研与优化日志

## 08:33 第十九班（批1：代码质量与评审 + 全类型雷达）

### 调研

常驻组抽样（A1×4/A3×4/A7×4/A10×5/A11×2）+ 今日批1 全 10 组 + 雷达 4 发，共 30 组采集、5 个新标深挖。最有价值发现：

- **sepia 2,714★**：StoryScope 研究（6.2 万篇）证明 AI 小说 93.2% 靠**叙事架构特征**检出、人工改措辞后几乎不降（95.5%→93.9%）——架构级 tells（主题说破/情绪身体化/成长式收束等）才是改不掉的破绽。我们的 aiflavor 只有措辞层，架构层是真空 → **本轮落地**。
- **pi-subagents 1,196★**：pi 的 Claude Code 风格子代理编排；`gate:"npm test"` 用确定性命令验证子代理而非再问模型——省 token 且更硬，进待深挖。
- **hermes-conductor 75★**：「Zero trust in self-reports」——diff/tests/commits 由控制者验证、绝不信任代理自报。我们的事件流计数鉴别谎报方向一致，获得验证。
- alibaba/open-code-review 38k★（确定性+AI 混合评审）验证我们「确定性检测行+模型评审」两层路线；tale 的 Arena 双模型对比 UI、memsearch（Markdown+向量统一记忆）、archgate（ADR 可执行规则）入雷达；短剧赛道 huobao-drama 15.4k★ 新锐活跃。

### 七专项巡检

- **A token**：既有八件套无回归；本轮落地件 1 直接是 token/注意力预算问题（wildcard 包挤掉教训）；pi-subagents 的 gate 验证（跑命令代替模型复查）记为下一步省 token 方向。
- **B 市场**：六源缓存 758 条（2026-09-16 抓取，5 天陈旧——按需刷新即可）；接入三问无新源通过，本轮不接。
- **C 任务类型**：16 个预置流程描述/引擎/参数逐一核对，昨日大修后无回归、无过时文案。
- **D 经验库**：77 条，流程规范 31（40%）仍偏科但小说类教训真实积累中；本轮新增架构层 AI 味检测后，评审可沉淀相关教训。
- **E 新 CLI**：目录 11 个不变；reasonix/fuxi/gitlawb/zero/embryo 本机全未装，按「先实测再入目录」纪律继续雷达跟踪。pi-subagents（pi 扩展）不适合入目录（是 pi 内部能力，不是新 CLI）。
- **F 禅道**：poll_enabled=false、产品档案 0 条、last_error=未配置——与昨日一致，本机未启用远端扫描，无积压可查；外部无新集成竞品。
- **G 产品**：无新毛病（今日上午并行代理刚完成 dispatch 大修+README 扩容）。

### 本轮落地（两件，均小而实）

1. **wildcard 包预算 + 教训保底**（`app/core/skills.py`，待深挖队列 #2 清账）：39 个 wildcard 通配包全文注入先把 9000 字全局上限吃光，项目教训排在末尾被整段截掉。两道预算：①wildcard 包单包 2400 字上限（定向命中包不受限，番茄/七猫全文注入）②教训保底——包区最多吃到「全局上限−教训长度」，教训永远完整注入。test_skill_wildcard_budget.py 5 项 + 既有 skills 域 28 项回归全绿。
2. **aiflavor 叙事架构层**（`app/core/aiflavor.py`，借鉴 sepia/StoryScope）：新增 `narrative_analyze()` 三类可确定性检测的架构级 AI 指纹——顿悟说教（终于明白/原来成长）、情绪身体化（心脏猛地/指尖冰凉/呼吸一滞）、成长式收束（释然/和解/归于平静，只扫尾 600 字防中段剧情词误伤）；告警时评审提示词追加情节结构追问（主题是否被说破/情绪是否只有身体反应/结尾是否靠想通收束）。test_aiflavor.py 扩至 10 项全绿。

### 验证

- 语法：py_compile（skills/aiflavor）通过；无 JS 改动。
- 全量 `unittest discover` **1053 项全绿（311.6s）**；test_selfupdate 3 项全绿。

## 提交与发版

- 功能与沉淀提交：`efb4423`（skills/aiflavor+两测试+knowledge+当日报告），已推送。
- 发版提交：`e34b51b`（package.json 0.1.23 + CHANGELOG + README relnotes，补记 v0.1.22 漏写的 changelog 节），已推送（并行代理顺推整支分支）。
- npm：`codebee@0.1.23` 发布成功；`npm view codebee version` 与 `dist-tags.latest` 均核验为 `0.1.23`（注册表 propagation 约 2 分钟）。

## 待深挖队列（更新）

1. pi-subagents 的 gate 验证：代码任务评审前先跑确定性命令（tests/lint），过闸才叫模型——省 token 且更硬（pipeline 域，等并行代理收口后实施）。
2. sepia 职场文体分场合规则：PR 回复/postmortem/技术文章的评审 prompt 增强（报告/代码评审域）。
3. 压缩摘要真正替换 CLI 请求上下文（并行代理域，勿碰）。
4. 生产 exact cache 接线、语义缓存命中边界与隐私隔离。
5. archgate 可执行 ADR（宪章从提示词级升级为规则级）远期。

## 10:30 用户点名：zai-org/ZCode 开源深挖

**结论**：功能面（桌面/Web/TUI/CLI）与 CodeBee 定位部分重叠但不可直接抄；真金是**治理体系**——机器可执行的架构基线守护。zcode CLI 本机无独立可执行（桌面宿主未进 PATH），按纪律不接 catalog。

**直接会话复查（用户追问）**：源码确认 Agent CLI 已有 `-p/--prompt`、`--output-format json|stream-json`、`--resume <session>`、`--continue`、`--cwd`，协议上可以成为 CodeBee 的直接会话智能体；但 `apps/zcode-cli/packages/cli/package.json` 当前为 `private: true`，本机 `Get-Command/where zcode` 均无结果，也没有可核验的公共 npm 安装链。接入三问：与现有 Codex/Claude/OpenCode 会话能力重合度高；源码可读但运行时是 Node 24 monorepo，不能作为轻量依赖直读；用户会搜 ZCode，但当前无法一键安装。结论：**先雷达跟踪，不造死链目录项**；官方发行稳定后按 `prompt/json/resume/cwd` 适配契约接入现有 runner。Apache-2.0 允许改造，但整仓复制必须携带 LICENSE、NOTICE/归属和修改声明，且会把第二套会话、权限、插件、升级系统引入 CodeBee，工程上不采纳。

**落地**：架构基线守护 Python 版三件套——
- `architecture-policy.json`：四层单向依赖（L0 基座 paths/settings/…/L3 入口 pipeline/main）+ maxFileLines 1200 + forbidCycles
- `scripts/architecture_check.py`：AST 建 import 图、三色 DFS 找环、层序单向检查、行数「历史最高」基线（存量千行文件不挡路、再涨一行就报）、`--changed` 只查变更文件、`--update-baseline` 刷新快照
- `.architecture-baseline.json`：首检 **136 条存量豁免**——5 条真实 import 环、123 条层序反向、8 个千行文件

**价值**：此前多代理并行写入的架构漂移（反向依赖、环、文件膨胀）全靠人眼和运气；此后每轮自动化提交前跑 `--changed`，漂移机器把关。开发过程抓到并修掉两个自检 bug：模块名归一（core.pipeline vs 裸名 manager）导致图断链漏环、lines 类豁免 text 含动态历史值导致比对失效。

**验证**：test_architecture_check.py 6 项（层序抓取/基线豁免/新环必抓/行数增长必抓/基线往返/真仓冒烟）+ 全量 unittest discover 1059 项全绿。

## 11:30 用户与同事实测反馈四连修（v0.1.24）

- **升级后没生效**（unknown api/选模型闪烁/老宠物总根子=旧进程滞留）：①升级完成自动重启（三道守卫：端口未知/版本未变/有任务在跑→回落手动）；②启动时端口被自家旧实例占用自动清场（命令行完整路径判定+杀树；别人的进程只温和一次）。
- **附件没读**：附件材料块从「请参考」改硬约束（先读再处理、不读视为未完成）；direct 双模板同步加强。全类型受益。
- **归档按钮不见了**：运行详情页补「归档任务/取消归档」显式按钮（原只有右键菜单）。
- **宠物关不了/拖不动**：关闭只销毁窗口不落设置被看护复活→先写 pet_enabled=False；拖动绑定了窗口上不存在的 scan_mark/scan_dragto（异常被吞）→geometry 坐标差值+16ms 节流。
- **更新说明分类化双语化**（对标竞品更新日志）：CHANGELOG 新节按「✨ 新功能/🛠 问题修复」分组；README relnotes 中英双语；前端 renderSu 新增 renderRelnotes 分类渲染。

提交：d180b44（功能）+ e690da2（发版），npm `codebee@0.1.24` 发布核验通过。全量 1088 项绿。

## 11:00 在线调度闭环与升级后自动生效

- **问题**：推荐路由此前主要依赖静态能力、人工优先级和历史胜负，缺少按任务类型/角色细分的真实成功率、延迟与成本反馈；运行详情能看到当次路由，却不能跨运行回放。npm 自升级完成后仍需手动重启，用户容易误以为新版本已生效。
- **落地**：用量台账新增 provider 维度，按精确维度→任务+角色→任务→全局逐级回退，成功率用 Beta(3,1) 平滑并计算 P50/P95 与平均成本；CLI 与模型链把这些指标作为软评分，健康、协议、密钥、显式绑定仍是硬约束，default 模型链继续保持人工顺序。新增脱敏调度事件台账与 `/api/dispatch/replay`，只保存 ID、候选、理由和终态，不保存 prompt、正文、密钥或文件内容。
- **流程**：代码任务先落确定性验证证据、再做模型评审；npm 升级成功、版本确实变化且无其他任务运行时自动重启服务，否则保留手动重启入口。
- **产品巡检**：修复桌宠拖动调用不存在的顶层窗口 `scan_dragto`、右键关闭未写回开关导致下次复活；端口清场只强制结束命令行精确匹配的旧 CodeBee，其他服务只报告占用，不发送关闭信号。
- **验证**：在线路由/缓存隔离/回放脱敏/provider 埋点/验证顺序 6 项，自升级守卫 4 项，Python/JavaScript 语法检查通过；全量测试 1065 项业务逻辑通过，Windows 默认 GBK 下 3 个架构测试仅因输出 `✗` 编码失败，UTF-8 环境复跑 6 项全绿。

## 11:45 附件消费闭环

- **根因**：任务创建只把附件路径写进上下文，普通文本/代码仍依赖模型主动调用读文件工具；运行中追加附件、换将和修复/修订轮更容易丢失原始附件约束。
- **落地**：文本、代码及 Office 伴生文本按预算预读进 prompt；图片继续走 CLI/内置模型原生视觉输入；不可预读格式记录明确状态，要求执行者说明缺失，禁止静默忽略。历史任务运行时自动重建附件块，运行中追加附件复用同一逻辑。
- **边界**：附件内容标为不可信资料，不能覆盖用户目标和系统规则；GB18030/GBK 正确解码，仅带 BOM 时尝试 UTF-16；旧附件块迁移只匹配完整固定标题，保留用户自己写的“附件材料”章节；项目记忆单独限额，不再截断附件。
- **全类型**：direct/code/review/serial 的首轮、评审及 code/content/serial 修复轮均保留附件上下文，覆盖代码、小说、文章、翻译、报告、邮件、演讲、短视频、技术方案、扫榜和禅道修复任务。
- **验证**：附件/直连/代码/连载专项 36 项全绿，架构检查与 changed 模式通过；全量基线 1092 项全绿，修复审查边界后将再次执行全量测试。

## 12:05 发送卡顿与历史附件迁移收口

- **问题**：短目标创建前的可选澄清调用最长等待 75 秒，前端只显示「先问几个问题」，容易被误判为任务排队或页面失灵；旧任务在空 context 时附件块位于字符串开头，刷新会重复注入。
- **落地**：澄清前端/后端超时统一收紧到 8 秒，超时或失败自动继续创建并恢复提交按钮；详情渲染先解析任务再更新归档按钮，消除潜在 `const` 暂时性死区；附件迁移兼容字符串开头的历史标题并补回归测试。
- **调度复核**：任务编译结果已在执行副本中统一覆盖难度与 engine，内容/连载实际绑定沿用 `easy/default/hard`；非法内部 payload 与 rubric 有安全默认值。
- **验证**：附件专项 12 项、Python/Node 语法检查、架构检查及全量 `unittest discover` 1094 项全绿。

## 12:30 删除任务响应优化

- **根因**：删除任务原先在 HTTP 请求线程同步递归删除所有运行日志、报告和步骤文件；历史运行目录较大时，前端要等 `rmtree` 完成才刷新，表现为点击后长时间无响应。
- **落地**：删除任务、单条运行和批量运行统一先从内存索引移除，再把目录原子改名为 `.deleting-*`，后台线程异步递归清理；原路径立即消失，失败不影响删除结果。前端增加“正在删除任务…”反馈。
- **验证**：删除/归档/运行状态专项 16 项通过；全量 `unittest discover` **1097 项全绿**，架构 `--changed` 通过。

## 12:10 第二十班（批5：检索/知识/浏览器 + 全类型雷达）

### 调研

31 组采集（常驻抽样 18 + 批5 全 10 + 雷达 3）。核心收获：**三个全新中文小说竞品**——chinese-novelist-skill 3,139★（偏好记忆跨会话学习用户喜好/三层问答/自动校验重写）、AI-Novel-Writing-Assistant 2,966★（卷战略+角色资源账本+状态回灌生产链）、AI-Novel-Writer 1,036★（**逐项目标审稿**：关键事件逐项核对已完成/未完成/待核实+正文证据；来源分层连续性材料）。claude-mem 94k★（会话持久记忆）入雷达；movo 113★ 把 dsh 变企业 Agent 平台（dsh 生态第三例）。检索/抓取组 AIHawk/obscura 灰色域只记录。

### 七专项

A token：八件套无回归，pi-subagents gate 验证仍在待深挖首位。B 市场：六源缓存 785 条（今日已刷新），无新源通过三问。C 类型：16 流程描述/引擎核对无回归。D 经验库：77 条，流程规范 40% 偏科持续（本轮文档类契约强化后可望沉淀写作教训）。E CLI：目录 11 个，reasonix/fuxi/gitlawb/zero/embryo 全未装不盲接。F 禅道：本机 poll 未启用无积压可查，与往轮一致。G 产品：并行代理本轮已修澄清卡顿/删除响应。

### 本班落地

sepia 职场文体分场合规则进 CONTENT_DELIVERY_CONTRACTS：weekly_report（结论先行/不指名甩锅/行动项带 owner+时间）、email（先答再铺陈/请求具体到动作与截止/篇幅∝利害删客套）、tech_proposal（问题开场/保留真实被否决方向/明确表态推荐/数字带条件口径）、doc（标题=结果/验收标准可测试/链接不整段复述）各 +2 条体裁硬规则，起草与修订全链路注入。test_content_contracts 新增 test_venue_rules_from_sepia。

（本轮改动与并行代理的「删除响应优化」同文件提交收编于 1c05b94，内容完整测试在内，按协议接受混归属。）

### 验证与发版

全量 `unittest discover` **1098 项全绿**；test_selfupdate 4 项绿。发版 v0.1.25（cdc04e5）：npm `codebee@0.1.25` 发布核验通过；GitHub 443 持续抖动推送多次重试，已挂后台长程重推（本地提交安全，下轮自动化兜底）。

### 待深挖队列（更新）

1. pi-subagents gate 验证（跑命令代替模型复查）——省 token 且更硬。
2. AI-Novel-Writer 逐项目标审稿：连载每章评审加「本章关键事件逐项核对+正文证据」。
3. chinese-novelist-skill 偏好记忆：跨会话学习用户喜好（经验库或任务档案层）。
4. AI-Novel-Writing-Assistant 角色资源账本（物品/伤因/转交追踪）。
5. 压缩摘要真正替换 CLI 请求上下文；exact cache 接线。

### 2026-09-21 操作状态中心落地

- **问题**：用户点击创建、删除、绑定或插件操作后，页面只有按钮短暂忙碌态；请求较慢或失败时缺少可回看的统一状态，容易误判为排队或卡死。
- **落地**：顶栏新增会话级操作状态中心；统一 `api()` 自动记录用户触发的写请求，显示进行中/已完成/失败、时间和截断后的错误原因；创建任务、删除任务使用明确标签，绑定、重命名、附件上传等路由补充识别；后台 `busy:false`、澄清探测和 GET 轮询不污染列表；最多保留 12 条，完成项可批量清除，失败项可逐条移除；移动端改为固定宽度自适应弹层，尊重减少动画设置。
- **验证**：`node --check`（app.js/i18n.js）、`git diff --check`、Edge CDP 回归（即时反馈、防重复点击、成功/失败状态、`operation:false` 和后台静默）通过。

### 2026-09-21 禅道产品选择与长任务卡死巡检

- **禅道产品档案**：原页面虽有产品/账号拉取接口，但新增产品仍让用户先盲填 ID，流程复杂且截图中的“未配置产品档案”无法自助恢复。现改为“测试并保存连接”成功后自动保存并拉清单，新增按钮直接从禅道选择产品；连接不可用时仍保留手填回退。
- **长任务根因**：本机两类运行均不是 CodeBee 队列排队：两个 mgmt 升级进程的日志停在 `npm install -g` 无输出，两个代码任务的 Codex 日志持续打印 `Reconnecting... waiting for network`，其中一个还在评审后的修复轮；Codex 默认单步 20 分钟，重复重连会绕过静默看门狗。
- **落地**：runner 增加重复致命输出提前终止（同一网络重连 5 次即杀树并进入既有换将链），智谱 Bigmodel anthropic 地址补齐已知 OpenAI 端点映射，避免把 `/api/anthropic` 错当 Codex `/responses` 入口。

## 14:00 待深挖四连落地（v0.1.26）

1. **gate 验证**（pi-subagents）：code 引擎 verify 失败的首轮跳过模型评审直接进修复（评审此时只能复述「验证没过」），连败 2 轮后恢复评审参与诊断——省一次评审调用且修复更快拿到明确目标。
2. **逐项目标审稿**（AI-Novel-Writer）：章节评审注入「本章大纲核对」块（beats+章末钩子），评审以 `<event_check>` 回逐项判定（已完成/未完成/待核实+正文证据），「铺垫了但没发生」不算完成；结果入 chapter_scores["event_check"]。
3. **资源账本**（AI-Novel-Writing-Assistant）：评审以 `<ledger>` 回本章道具/伤情/承诺/伏笔增量，达标后追加 `.codebee/resource-ledger.md`，下一章起草注入「资源账本」区防跨章穿帮（不进圣经稳定块，前缀缓存纪律不破坏）。
4. **偏好记忆**（chinese-novelist-skill）：`core/prefs.py` 记住上次创建的类型/轮数/阈值/赛马/章节参数（只记参数不记内容，数值钳制防脏数据），新建表单类型与参数预填最近选择；GET /api/prefs + 创建时记录。

测试：test_deepdig_mechanisms 6 项（gate 首轮跳评审/恢复评审/ledger 往返/越界拒写/解析/占位符）+ test_prefs 5 项；全量 **1109 项全绿**。

## 17:52 第二十一班（批3：计划/spec/长任务 + 全类型雷达）

### 调研执行

- 按 `keywords.md` 实际清单跑完 A1-A12 常驻 71 组、17 点对应的 B3 全 10 组和 C 雷达 31 组；每个 GitHub 关键词均做 stars 与 `created:>2026-03-01 sort:updated` 双轮，GraphQL 每批 8 组、批次间隔 7 秒，合计 112 组、123 个去重候选。结果重合即不翻余页。
- C 雷达覆盖 awesome 清单、8 个 topic、框架/CLI/禅道周边和 npm；六源市场缓存共 785 条且均在今日刷新。PyPI 搜索页未返回可解析条目，GitHub Trending 页面仅返回导航项，作为来源限制记录，不据此造结论。
- 全类型复查覆盖 15 个内置流程以及禅道生成的代码修复任务：代码看评审/测试 agent，小说与连载看 story/consistency，文章与短视频看发布管线，翻译看质量工作流，对话看 memory/session，邮件/汇报/演讲/方案/调研/文档/简历/扫榜均有对应关键词。

### 有效增量与对比

- **CONTINUUM（28★，新锐）**：语义检查点不是整段对话快照；用幂等动作账本拒绝恢复后重复执行外部副作用。CodeBee 已有断点恢复和 CAS 状态收口，但缺跨插件/禅道/发布动作的统一幂等键，列入后续路线图。
- **maestro-flow（556★）**：按意图自适应生命周期并用知识图谱回灌流程。CodeBee 本轮已将难度、风险、模式编译为动态工作流，图谱层与文件式经验库重合且会增加存储复杂度，暂不引入。
- **keep-the-why（158★）**：把被否决方案及原因作为 Git 版本化 Markdown 记忆。CodeBee 的 findings/evidence/knowledge 已是文件真源，差量是“否决理由”结构化，可在任务档案演进时吸收。
- **confdiff（41★）**：结构化配置 diff 只显示键和值变化。可用于后续 diff-only 评审降低噪声；当前评审链已有 git diff 和确定性验证，先跟踪。
- **wechat-article-pipeline-skill（21★）**：文章优化、配图、排版及多平台草稿一体。CodeBee 文章流程已有写作/评审，缺少发布适配；因需要第三方账号与平台接口，蒸馏“交付后发布清单”，不直接安装。
- 复查 **Continuous-Claude-v3 / acpx / vCache / adaptive-llm-gateway / claude-mem / planning-with-files**：仍活跃，方向分别对应隔离上下文、会话协议、语义缓存、跨会话记忆和持久计划；本轮无超出现有路线图的新机制。

### 七专项巡检

- **A token**：三段压缩、token meter、预算熔断、cascade、经验召回、会话复用、diff 评审与 `_shrink_context_block` 无回归；本轮落地低风险短链、按难度评审人数/修订轮、确定性验证优先、思考程度实传。语义缓存继续等隐私隔离和生产精确缓存接线后再做。
- **B 插件市场**：六源为 zcode 26 / anthropic 310 / anthropic-skills 5 / claude-skills 99 / clawhub 195 / cocoloop 150。三问结论：新候选与现有能力重合高、部分不能直接安装、用户主动搜索概率不足，本轮不接新源；保留白名单与逐跳 SSRF 闸门。
- **C 任务类型**：直连一步；代码按风险在“实现→确定性验证/评审→最多一次针对修复”和深链之间选择；邮件/周报/翻译省大纲且单评审；文章/演讲/短视频/文档/简历按难度决定大纲与轮数；研究/技术方案/小说保留深链；连载保留圣经、逐章与全局门禁；扫榜走直连。
- **D 经验库**：77 条，分类为流程规范 31（40.3%）、节奏爽点 20、情节逻辑 11、人物塑造 8、一致性 4、文笔风格 3；流程规范仍偏科。本轮只将可复用的“语义检查点/幂等副作用”写入知识库，不把竞品链接当经验。
- **E 新 CLI**：目录 11 个；DeepSeek-Reasonix/FuXi/Gitlawb/zero/Empryo 本机仍未检测到，不建死链。`acpx` 与 Kimi Code 新仓继续作为协议/发行雷达。
- **F 禅道**：`poll_enabled=false`、产品档案 0、`last_error=未配置产品档案`，本机未启用扫描，无法产生真实积压；设置页产品选择与并发满立即失败路径已有测试。Atlassian/AI 工单竞品有增量，未发现优于现有“Bug→修复→合并→resolve→评论/通知”闭环的机制。
- **G 产品**：修复语言切换覆盖端口标题导致桌宠仍重复开窗；修复 Anthropic 兼容协议收到 OpenAI `reasoning_effort` 后可能 400；代码/连载报告正确显示快速、自动、专家、手动模式。

### 本班落地与验证

- 创建任务新增快速/自动/专家/手动与独立思考程度；直连会话支持自动、指定厂商、指定模型，选择只存当前任务，指定不可用明确失败；直接执行类型本地推荐切换，不额外耗模型调用。
- 全类型动态流程、ETA/P90 与运行剩余时间、默认不排队、重复网络重连早停、失败运行收口、附件预读、成果噪声过滤、Aider Git 可读性诊断及桌宠精确窗口复用均已接入实际路径。
- 最终全量 `unittest discover` **1124 项全绿（186.545s）**；动态流程/会话路由/自更新专项、架构检查、Python/JavaScript 语法与 `git diff --check` 全过；语言切换、微信群聊汇报、禅道设置、章节评审四组真实浏览器测试全绿。

### 待深挖队列

1. 对禅道回写、内容发布、通知等外部副作用引入统一幂等键与动作账本，配合语义检查点恢复。
2. 让压缩摘要真正替换 CLI 请求上下文；完成生产 exact cache 后再评估按任务隔离的语义缓存。
3. 结构化 diff 用于 JSON/YAML/TOML 配置评审，减少纯文本噪声。
4. 将“被否决方案及理由”写入任务档案，避免后续智能体重复走死路。
