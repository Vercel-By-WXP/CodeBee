# 2026-09-22 竞品调研与优化日志

## 00:00 第二十四班（批7：中文/网关/本地/办公）

### 调研

30 组采集。核心收获：**openhuman 40,002★ 新巨型标**（本地优先 agent harness：记忆+编排+工作流，与 unsloth 76.5k/anything-llm 66.3k 同列本地三巨头）；讯飞 astron-agent 9k + astron-rpa 5.6k（企业 agentic workflow + Agent-ready RPA）；Yuxi 7.2k（可私有部署多租户知识智能体平台）；**BidCraft 标书匠**（对话式标书编制——新任务类型灵感：标书/投标文件，进 keywords 备选）；1Panel-Gateway/FailoverAI/CosyRedactGateway 网关新锐。中文关键词直搜命中弱（B7 五组空），靠雷达-中文新锐补位。

### 七专项

B/D/E/F 已过（市场 785/教训 78/CLI 候选未装/禅道未启用）；A 项落地见下；C/G 无新毛病。

### 落地

T2.2 幂等调用精确缓存补全接线（§07 token-cost 收尾）：连通测试已接 24h；补编排者三处——连载大纲（尝试 1 TTL 1h、尝试 2 故意旁路保换样本语义）/代码计划 1h/评审大纲 1h。断点续跑/重跑同任务规划直接命中，省一次全量编排者调用。test_planner_cache_wiring 3 项。

### 验证与发版

全量 unittest discover **1141 项全绿**（1 skip 平台项）；test_selfupdate 6 项绿。v0.1.41 npm 发布核验。

## 02:01 第二十五班（批2：学习记忆与自我改进）

### 调研

30 组采集。核心收获：**codegraph 71,711★ 巨型标**（预索引代码知识图谱，代码变更自动同步——与我们知识库边界清晰互补）；MemRL（运行时 RL 作用于情景记忆，与 outcome 加权同向）；mengram 程序性记忆（从做中学——我们空白，雷达）；pro-workflow 2.9k（自纠错记忆跨 50+ 会话复利）；projectmem（重蹈覆辙前警告 agent——事前警告与注入式教训互补）；graphiti/cognee 31k 双雄复查稳定。

### 七专项

B/C/D/E/F 已过（市场 785/16→17 流程/教训 78/CLI 未装/禅道未启用）；A/G 无新毛病。

### 落地

新增「标书编制」任务类型（BidCraft 灵感清账）：bid_doc 流程（五维度/门槛 7.5）+ 投标经理契约（逐条对齐评分标准/资质不虚构/先结论/废标自查）+ dispatch writing 维度 + 中英文案。test_bid_flow 3 项。

### 发版

全量 1144 项绿；v0.1.42 npm 发布核验。

## 04:01 第二十六班（批4 复跑：治理/安全/人机协同）

### 调研

批4 轮换周期重跑，标的与上轮高度重合（微软治理包 +1k、edict/stop-that-shit/tradememory 等均已入库），无新重大标的；baml 9.2k/plano 7.1k 复查活跃。

### 七专项

B/C/D/E/F 已过（市场 785/16 流程/教训 78/CLI 未装/禅道未启用）；A 项确认 T3.1 廉价分流已在 dispatch 落地（tier+price 按 difficulty 评分）；G 无新毛病。

### 落地

程序性记忆 MVP（mengram「从做中学」）：LEARN_PROMPT 扩展双类记忆——规避性教训 + 已验证有效做法（「做法：」前缀，高分一次通过优先提炼）；clean-pass 运行此前学不到东西的盲区补上。test_procedural_learn 3 项。

### 发版

全量 1147 项绿；v0.1.43 npm 发布核验。

## 06:00 第二十七班（批6 复跑 + openhuman 深挖）

### 调研

批6 轮换复跑+openhuman（40k）深挖完成：三支柱（Memory Tree 评分树+Obsidian 镜像/TokenJuice 工具输出压缩 80%/分裂脑快慢编排）——差量=工具输出预压缩（进待深挖）与可编辑记忆镜像。新标：**Google A2A 官方仓库 25.9k 新巨型标**（协议从库实现升官方主体）、sia 2.2k（自改进任意 AI 系统，待深挖）、llm-as-a-verifier 3.3k（无需求文档的细粒度反馈）。

### 七专项

B/D/E/F 已过；A（TokenJuice 差量记入待深挖）/C/G 无新毛病。

### 落地

经验库 kind 分型：持久层稳定 token（procedure/lesson，与并行代理撞车实现统一收编）+ view() 翻译「做法/教训」+老数据派生兜底 + UI 绿徽章。test_lesson_kind 2 项。

### 发版

全量 1149 项绿；v0.1.44 npm 发布核验。

## 08:01 第二十八班（批1 复跑：代码质量与评审）

### 调研

批1 轮换复跑：主体标的重合（mira/pr-af 活跃）。新锐入雷达：jev-review 196★（MCP 持续评审）/reslop 118★（专评 AI 生成代码——新角度）/mr-agent 99★（GitLab MR 多代理+CI 自愈，禅道同域第三标的）。

### 七专项

B/C/D/E/F 已过；A 项落地见下；G 无新毛病。

### 落地

read_file 头尾保留中段省略（TokenJuice 差量清账）：64KB+ 文件头 44k+尾 16k+省略标注，报错结论不再被截掉；尾段多字节剥头防乱码。test_read_elide 4 项 + test_builtin_agent 整文件首入库（14 项）。

### 发版

全量 1153 项绿；v0.1.45 npm 发布核验。

## 20:30 用户反馈三连修（不发版，攒批发）

- **顶部滑动加载线屏蔽**（用户拍板）：#request-progress 整个 display:none——禅道内网不可达时后台 POST 挂 15-30s、线一直滑观感差；操作反馈由「操作状态」中心承担不丢信息。
- **禅道页卡加载**：进页自动 ztFetchCatalog 拉产品/账号清单，禅道不可达时两个 POST 无前端超时干等——改 busy:false+silent+45s 超时，按时收口报人话。
- 左栏「禅道」旁转圈=同期按钮 request-busy 态，随以上消失。

## 12:00 第二十九班（批5：检索/知识/浏览器 + 全类型雷达）

### 调研

约 85 组采集（常驻 A 组全跑 + 批5 轮换 + CLI/禅道周边雷达；GitHub search API 30/min 限流，首轮撞限后补跑空档组）。核心收获：

- **multica 51,033★（新巨型标）**：「Agents that show up on the board」——派活给 AI 编码 agent 如同派给同事，agent 认领 issue、汇报进度、抛阻塞、交回待评审；自托管 + 支持 **26 种 agent CLI**。与我们同为多 CLI 编排，但组织视角是「看板/issue」而非「任务类型」。
- **sandbaseai/sandbase-harness 649★**：本地优先自托管 agent runtime + MCP bridge——沙箱会话、**凭据保险库**、权限策略与审批、**审计与可续传事件流**、本地 Console。
- **2026 下半年编码 agent 的共识信号：不接受 agent 自报完成**——同族四例（agent-delegate「抓 exit-0-无改动/越范围编辑/半成品收工」、scopebond 范围检查点、shadow 证据门禁、agent-cortex「Candidate-Verification-Independent Review-CompletionRecord」）集中出现，与我们事件流计数鉴别谎报 + 跨厂商评审 + 待裁决完全同向。
- 同形态新锐：**OmniTerm**（多 CLI 一页看住并驱动）、**agent-pane**（桌面：对话+编辑器+终端+git+浏览器同屏，「应用不了的编辑等批准」）、**CPA-Manager-Plus 3.5k / adhdev / comandos / szpont-machen**（多 CLI 面板 + 用量 + 会话归档群）。
- 禅道生态四例：**zentao-legacy-mcp**（老版 session API 跨客户端 MCP，87 工具+跨实体全文检索）、**easysoft/zentao-cli**（官方）、**test-defect-retrospective**（缺陷复盘→产品/研发/测试三视角报告）、astrbot 缺陷日报插件。
- 各预置类型覆盖：小说域（Novel-Engine「volume-aware」、novel-studio「事务性 canon」、web-novel-pacing-analyzer）；代码域（agent-delegate/scopebond/reslop 等同族）；文档/写作域（beagle、anti-slop-docs 等 skill）；邮件/汇报/PPT/短视频组仍以学生项目为主，无重大标的。

### 七专项

- **A（token 节约）**：对比基准齐（三段压缩/token_meter/预算熔断/cascade/经验召回/会话复用）。新差量=**opencode-metrics** 的「tokens/sec + TTFT + 缓存命中」——我们 token_meter 已累加 cached 但 UI 未露出；记入借鉴方向（用量页加缓存命中率）。本轮无新压缩机制，压缩侧维持既有。
- **B（插件市场）**：六源连通（zcode/anthropic/anthropic-skills/claude-skills/clawhub/cocoloop）；新增候选均为聚合清单/同步器（beagle/awesome-ai-plugins/skill-manager/dsh-plugin-hub），三问（重合度/可直读性/用户会搜吗）不佳——**不新增市场源，雷达跟踪**（与上轮 anbeime/skill 结论一致）。
- **C（任务类型）**：16 个预置流程逐个核对 name/goal_hint/note → 发现 **2 处中英文案与实际不符**（见下 G）。i18n 覆盖补齐后 16/16 完整。
- **D（经验库）**：实盘 79 条，分类分布 流程规范 33 / 节奏爽点 20 / 情节逻辑 11 / 人物塑造 8 / 一致性 4 / 文笔风格 3——偏科从 61% 降到 **41.7%**，仍需继续分散；kind 字段实盘尚未出现 procedure（程序性记忆为 04:00 刚落，等真实高分运行沉淀）。
- **E（新 CLI）**：本机 which 探测 24 个候选，结论无变化——reasonix/fuxi/gitlawb/zero/empryo/goose/crush/herdr/openclaw/zcode/gemini/cursor 均未安装；已装 10 个（codex/claude/qwen/opencode/aider/kimi/grok/mimo/pi/dsh）都在 catalog。
- **F（禅道）**：配置健康（product=96、后端仓库 E:\GitLab\cbc\mo-so、owners wuxinping/xiangdong、auto_merge/auto_resolve/triage_ai 均开）；**poll_enabled=False（用户未开定时扫描）**，故无 Bug 积压可查——wiring（automation.tick → zentao.fire_due）在位。
- **G（产品巡检）**：3 项（详见「落地」，其中 2 项为本轮修复、1 项为待办）。

### 落地

1. **叙事节奏/钩子确定性检测**（借鉴 web-novel-pacing-analyzer）：aiflavor 新增 `pacing_analyze`——段落淤积（段均长/超长段）、对话占比、**开篇 300 字冲突信号**、**章末 200 字悬念信号**，五条告警；`report_line` 按流程类型（NARRATIVE_FLOWS=novel/serial_novel/video_script）下发。
2. **连载逐章评审补挂确定性检测**（产品巡检发现的真实漏挂）：`_run_serial_review` 的评审提示词**从未**带过 AI 味/叙事架构/节奏检测行（`_run_content_review` 一直有）——连载是我们的旗舰场景，逐章节奏与钩子恰恰最需要这条参考线。抽出 `aiflavor.inject_into_prompt` 作为**单入口**，单稿件与连载逐章共用；插入点固定在「## 待评审稿件」之前（与技能包/圣经/知识库注入同位，保前缀缓存稳定），命中才追加、只作参考不扣分。
3. **i18n 两处失效描述修正**（专项C/G）：`rank_scan` 的 note 早已改成「七猫+番茄」双源，英文键仍是旧单源文案（英文界面下描述与实际不符）；`code` 流程 note 缺英文键（英文界面回退中文）。两处补齐后 16/16 流程文案中英完整。

### 发版

全量 unittest 绿；v0.1.50 npm 发布核验（见提交记录）。

### 遗留（未修，如实记录）

- **架构检查全量扫描 2 条存量违规**（非本班引入，来自 b98c637）：
  - `layer: hooks(L99) -> paths(L0)`——`hooks.py` 是新模块但未进 architecture-policy 分层；因它被 L1 的 store 与 L3 的 pipeline/main 同时依赖，且自身依赖 L0 的 paths，**任何分层都会产生反向依赖**（放 L99 是策略里「未分层模块」的既定语义）。要真正消除需要调整 policy 的层归属或其 paths 依赖。
  - `lines: app/core/manager.py 1860 行（上限 1200，历史最高 1851）`——883e93b 增长后未更新基线。
  - 注意：`scripts/architecture_check.py --changed`（提交前闸门）**看不见这两条**（只扫变更文件），所以它们在 HEAD 上静默留存至今——已记入报告，待有主者处理或更新基线。
- **本班不再动 npm 版本**：并行代理正在同一仓库推进「分卷」收尾（i18n/flows/planner 在制品）与本轮 0.1.50 发版周期，`package.json files` 是 `['app', ...]`（npm 打包按文件系统而非 git），我若此刻发版会把其未提交的在制品一起带给用户。本班代码已提交并推送（7d8a767），随下一次发版落地；**当日发版已由并行代理完成（v0.1.49）**。

### 沉淀同步

- keywords.md：新增 **A13 组「多 CLI 面板 / 用量指标 / 运行守卫」（7 组）**——本轮实测这一簇集中命中（OmniTerm/adhdev/CPA-Manager-Plus/szpont-machen + agent-delegate/scopebond/shadow/agent-cortex「不信任自报」族），原词库没有专门覆盖面；常驻组 71 → 78 组，总库 140 → 147 组。
- keywords.md 检索纪律修正：GitHub **认证搜索接口限流是 30 次/分**（本轮 85 组无间隔连打撞 403、补跑才拿全）——一律 sleep 4 秒起，原文档只写了未认证的 6-8 秒。


