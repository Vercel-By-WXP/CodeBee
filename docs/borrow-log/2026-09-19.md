# 竞品调研与借鉴日志 · 2026-09-19（夜间第一班）

## 调研
周六轮换批7+常驻抽样；深挖 planning-with-files（26,981★，三文件计划/每轮注入/防篡改/Stop gate/盲测3胜）。新沉淀 cognee（30.8k★ AI 记忆平台）、agency-orchestrator（2.3k★ 中文一人公司）、deep-eye、LocalAGI。cc-sdd 停更降级出队列。

## 六专项
A token：PWF 的 KV-cache 稳定注入与我们既有前缀缓存纪律同向，无新差量；批7 未现新标靶
B 插件市场：本轮未扫源（时间预算），下轮补
C 任务类型：菜单/预估行状态良好，无新发现
D 经验库：新增教训「长任务防目标漂移：计划落盘+每步注入进度头」（skills.upsert_lesson 成功）
E 新 CLI：本机探测 7 个候选均未装，无接入
F 随手修：无

## 落地（借鉴 planning-with-files）
- `app/core/pipeline.py`：①`_write_task_plan()` 计划落盘 .codebee/task_plan.md（无步骤跳过、越界拒写、失败静默）；②代码任务多子任务时每个子任务提示词注入「计划进度（第 i/N 项）+已完成清单+不重做」
- 测试：tests/test_task_plan_file.py 3 项；全量 **806 项全绿**
- 注：Mimosa 本轮拦下内联落盘写法两次，函数化（pathlib resolve+parents 守卫，与 _write_task_spec 同构）过审——「任务档案三件套」(spec/plan/evidence) 落盘统一走此模式

## 提交/发版
见文末补记。

## 补记：提交与发版
- feat(plan)：a073450（4 文件 +106/-1，五道关全过，806 测试全绿）已推送
- chore(release)：f7a0049 已推送；**codebee@0.1.12 已发布 npm**（publish 确认，view 待传播）
- 下班。明晚重点：agency-orchestrator 深挖、插件市场源扫描补账、扫榜数据源调研（路线图外部依赖项）

## 上午收尾（主线代理 · 11:20）

- **真机验收收官（用户登录态，七猫）**：①profile 迁移（data→~/.codebee）后重连**免扫码直接 connected**——登录态迁移无损；②发章端到端：navigate 30s 对 book-upload 重页偏紧致一轮失败 → flow 的 navigate 步支持 per-step timeout 并放宽缺省 45s → 重发 **upload_chapter OK（第2章，表单填好停人工确认点，auto_submit=false 纪律）**。至此七猫「建书+发章」真机双通（建书 8af1ee4 已由校准代理落地）；番茄登录态已迁，建书流程选择器仍待实抓校准（fanqie flows-*.json 未出）。
- **profiles 迁出仓库（f542348）**：995MB 浏览器运行时数据搬至 ~/.codebee/publish_profiles；坑三枚——Windows 跨盘 replace 不行要 shutil.move；残留浏览器（fanqie Edge 自 22:58 挂了一夜+qimao 校准残留）锁文件，「另一个程序正在使用」要先按命令行 grep user-data-dir 揪进程 taskkill；robocopy 对目的端已有副本「跳过≠删源」，收尾要 //MOVE+抽样 cmp 验证再 rm。残留壳目录（<10MB 句柄锁文件）留给后续自然清理。扫描噪声根治。
- **settings_v2 消费路线闭环**：压缩总开关（env 或 v2 任一）/压缩调参（threshold+retain，0=不保留语义区分 None）/goal 轮数上限三消费者接线；describe 带字段元数据；GET /api/settings-v2 + POST mutate（revision CAS 409）；设置页引擎调参卡 schema 驱动；消费测试 7 项。至此 orchestrator/budget/cascade 三命名空间全键有消费（budget/cascade/outline_timeout 为夜班已接）。
- **curl 中文 JSON 坑**：Git Bash curl 会把 heredoc 中文转 GBK 发出（服务端「请求体必须使用 UTF-8」拒收）——API 冒烟含中文 body 一律走 Python http.client。
