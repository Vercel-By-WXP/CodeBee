# 2026-09-25 竞品调研与优化日志

## 00:30 用户点名清账班·第四班（终清：挂账清单全清）

> 用户「剩下的继续搞完」。挂账清单最后三件可做项全部落地；⑨Office 编辑态唯一遗留（依赖选型呈用户，见尾注）。零新采集，纯清账班。

### 落地（三件，详见 knowledge.md 本日 00:30 节）

- **ARIS 评审证据锚**：两个 critique 提示词 issues 增 quote 字段（逐字摘录+编造降档威慑）；`_anchor_issues` 确定性校验三态（锚定/未锚定降档殿后/老格式灰度）；归一去空白+剥首尾标点；连载与内容修订面换 `_major_lines` 锚定优先排序+⚠计数行——「模型只提议、锚定由规则裁决」进评审域
- **continuum 陈年承诺督促**：`_ledger_watchlist` 按章戳解析资源账本承诺/伏笔（已了结剔除、同名以最后出现章为准），≥3 章未推进 → 起草面追加「⚠ 陈年承诺」清单督促推进/回收/留白。资源账本与章戳此前已在，本班补的正是 continuum 差量的账龄督促
- **ECC evolve 教训晋升**：`skills.evolve_lessons`（注入≥6+可信≥0.75+归因≥1）聚成用户包草稿、默认停用待人工审阅；POST /api/skills/evolve。同步勘误：ECC 差量「注入条数上限」系误记（MAX_LESSONS_INJECT/KNOWLEDGE_MAX_INJECT 早已双存在）

### 验证与收尾

test_final_borrowings 15 项绿（锚三态/督促边界/evolve 门槛与停用态）；相邻域 review_fallback/reviewer_error/quality_gates/serial_review_resilience/lesson 三件全绿；py_compile pipeline/skills/main 过。planner/volumes/bookmeta/UI 并行在飞不碰不发版。

### 尾注：⑨Office 编辑态（唯一遗留，待拍板）

只读预览已全（docx/xlsx/PDF）。编辑态选型建议：xlsx=Univer（UMD 可无构建引入但体量 MB 级）、docx=Tiptap（需构建链）。当前零前端依赖的静态架构下二者都是重决策——建议维持远期，或单独排一班做「vendored 精简版」评估后再定。
