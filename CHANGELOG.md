# 更新日志

CodeBee 的用户可感知变更记录。发布新版时：最上面加一节，并同步 README 里的
`<!-- relnotes:start -->…<!-- relnotes:end -->` 段（那段会被 `npm view` 的
README 元数据带回，供老版本在「发现新版本」时展示新版更新内容）。

## 未发布

- **内置智能体补齐四大基础工具（P0）**：处理真实代码库的短板逐个补上——①局部编辑 `edit_file`（精确查找替换，默认要求命中唯一、可 replace_all，CRLF 文件保持换行风格）；②追加 `append_file`（bytes 级追加，日志/记录不必整份重写）；③文件管理 `fs_manage`（删除/移动/重命名/复制，目标已存在一律拒绝覆盖）；④全文搜索 `search_content`（grep 式正则，命中带「路径:行号:」，自动跳过二进制与 node_modules 等依赖目录）+ `find_files`（文件名通配）。同时 `read_file` 支持 `offset`/`lines` 按行号分段读取——超 64KB 的大文件不再只有「头尾保留、中段省略」一条路。所有新工具沿用工作目录锁死守卫。顺带修掉一个真 bug：Windows 下写文件默认把换行符 `\n` 翻译成 `\r\n`，文件本就含 CRLF 时被二次翻译成 `\r\r\n` 写坏（edit_file/write_file 均已禁翻译，写什么落什么）。
- 调度层加固：绑定链上的「纯模型条目」（只配模型名、没挂供应商）起跑前即被点名——所有 CLI 起跑日志新增凭据模式审计行（绑定链注入/无注入/本机默认，密钥值绝不入日志），缺凭据+登录闸门类错误按 CLI 附修复指引；不再只有 claude 有这层观测（2026-09-25 Mac claude 实案的三层加固通用化）。
- **Mac 桌宠不再是「白板蜜蜂」**：macOS 的 Tk 没有逐像素窗口透明（`-transparentcolor` 是 Windows 专属），蜜蜂的矩形底板整个露出来还带系统边框（实拍=一块白板）。现在非 Windows 平台把底板画成深色卡片（对齐悬停清单面板色），桌宠呈「桌面小组件」形态；睡觉/失联的状态滤镜改为只作用于蜂体（原先整帧压暗靠键色噪点兜底，卡片底兜不回来会浮出一块更暗的小框）。Windows 逐像素透明不受影响。

## v0.1.69（2026-09-25）

- **智能体目录新增三款 CLI（第三批接入）**：Google **Gemini CLI**（每日 1000 次免费请求额度，凭据走个人 Google 账号登录）、腾讯云 **CodeBuddy CLI**（`cbc`，claude 兼容系）、字节开源 **Trae Agent**（`trae-cli`，PyPI 无包故走 uv 从 GitHub 直装）。三者均支持目录管理、一键安装/升级与无头编排（走各 CLI 自带登录态）；因其凭据环境变量与 CodeBee 的两种 wire 协议不对应，暂不支持「CLI 绑定」链——配置了也会被明确拦截提示，不会出现「链显示活着、实际走 CLI 默认模型」的假绑定。
- 美团侧经调研无官方 CLI 产品（LongCat 是模型家族+CatPaw 平台）；LongCat-2.0 模型可经支持自定义 provider 的现有 CLI 作为上游接入，无需新条目。

## v0.1.68（2026-09-25）

- **小说任务初稿不再「边写边自我审查」**：起草契约新增「成文分三步」——先把不设限的情节/画面/台词念头快速倾倒成素材清单（狂人），再挑主线组织结构（建筑师），最后按结构成文（木匠）；初稿期不做质量审判，质量把关仍由既有评审链承担。此前契约两条全是收敛导向，发散步骤缺失，点子容易在起草半路被掐死。
- **商务邮件回复不再漏答对方的问题**：邮件契约新增线程级交付规则——回复往来邮件时逐条回应对方提出的每个问题与请求（可用清单），不漏问、不回避；行动项落到具体动作+负责人+截止时间。此前只有我方发件视角的请求规则，缺收件回复视角。
- **README 任务类型清单过时修正**：「15 种」→17 种（补缺陷复盘/标书编制）、扫榜选材平台描述从两平台更正为七猫/番茄/起点/纵横四平台——24 班扩源与 22/25 班加类型后文档未跟上。

## v0.1.67（2026-09-24）

- **连载任务超时不再要人守着手动重试**：「任务总时限已到」此前是死终态——总时限默认 1 小时，供应商只剩慢链时一批十二章天然跑不完，用户只能反复点重试（2026-09-24 戍边骑奴 9-20 案）。现在超时的连载任务与失败一样自动续跑（继承已完成章、延迟 5 分钟退避、次数上限照旧）；同因止损对超时做了进度感知——错误签名恒为同一句，但本轮比上轮多过审了章就不算「退避没换来结果」，两轮一章未进才止损。
- **任务总时限建完任务可以改了**：任务信息弹层新增「任务总时限」（30 分钟～24 小时档位），空闲时可选，下次运行/重试按新预算重新给满；`/api/tasks/<id>/params` 同步收 `timeout_s`（统一钳制 1 秒～7 天）。此前它只在建任务时按默认 1 小时写死，没有任何事后入口。
- **分卷连载大纲步崩溃修复（norm_volumes 缺实装）**：`aea5632` 半提交只把调用放进了 planner、实现从未进 volumes——带分卷的连载大纲步必炸 `AttributeError`（同晚四连败实案），不分卷路径不触发故潜伏两天。补齐实现（收模型卷名只留本批覆盖卷；现成卷规划表优先对齐主链边界）+ planner 透传 + 三组回归。
- **qwen 注入器内闸：anthropic 面 URL 拒写**：`_sync_qwen_settings` 本体加协议闸，anthropic 面（如 `.../api/anthropic`）绝不再写进 `OPENAI_BASE_URL`——史上某版写进去后 qwen 拿 openai 协议打 anthropic 路由、网关恒 404 被误判「网关限流」，每轮运行起点白烧 2-3 次重试（今晚实案）。外层调度已有协议闸，这里钉住注入器本体防复发。
- **「这个任务当时用的哪一版流程」有账可对了（流程修订钉住）**：建任务时把当时流程定义的内容指纹（digest）钉进任务快照；之后改流程再续跑/重试老任务，运行会留下明确诊断（「流程在任务创建后已修改，固化参数不受影响」）并计入运行警告——此前历史任务缺失字段的兜底取值会静默吃到新流程定义，出了结果差异只能靠猜。改流程的图标、说明文字这类展示位不制造漂移噪音。
- **每个步骤现在都记「实际用的是哪家上游」**：步骤起跑即落一行绑定速记（`厂商@网关地址` 形态，密钥绝不入账）。烧错配额、模型漂移、换将补位之后「这步到底走的谁」直接对账步骤行，不必再翻日志文本找审计行；计划选路（route_plan）与实际执行从此分开可见。
- **「已停止」不再冒充「真的停稳了」**：取消/超时杀进程树时，如果没能确认整棵树清空（Windows taskkill 卡死、POSIX 组杀失手），步骤会如实标注「进程树未确认清空：孙进程可能仍在运行」——只作诊断提示，不改变取消/超时的终态语义，也不影响密钥冷却等记账。
- **步骤卡能直接看到「实际用上游」徽章了**：模型徽章旁边新增上游徽章（虚线胶囊，`厂商@网关` 形态）——运行中和终态步骤行都不用翻日志就能对账这步走的谁。
- **两个窗口同时改任务参数，后写的不会再悄悄覆盖先写的**：对话条的编排模式/思考程度/对话模型现在带修订号提交，别的窗口先改过就拒绝过期写入并提示「参数已被其他窗口修改」，界面随后自动同步最新值——此前是「我以为改成功了，其实早被另一台设备覆盖」的静默假成功。
- **附件换过了，续跑不再装不知道**：附件（截图/文档/表格）收进任务时记内容指纹；续跑/重试时对账当前文件，被换过、删过都会在运行警告里点名（「附件在任务创建后已变化：xxx；本次执行使用当前文件内容」）——此前任务吃到「不是当初那份附件」毫无声息。老任务没记过指纹的不做推断、不打扰。
- **成品弹窗看得开 PDF 和 Excel 了**：点成品文件名预览，PDF 逐页渲染（按窗口宽度适配，大文件封顶 20 页并提示下载看全）、xlsx 表格带工作表页签（200 行 × 40 列内直接读，超出提示下载）。渲染器首次打开时联网加载（与 docx 预览同款，平时零请求）；离线或加载失败自动回落为下载提示。老式 .xls/.doc 二进制不在预览之列。

## v0.1.66（2026-09-24）

- **CLI 配置同步失败不再静默放行；同一上游判定口径统一**：把「CLI 绑定」链首落进 CLI 自家配置这一步，此前写盘/注入失败会被静默放过——任务带着旧配置起跑，撞出「2 秒 UnknownError」这类莫名错误还得靠人查。现在同步失败直接判 `ENV_BLOCK` 并给人话提示（「CLI 绑定配置同步失败，已阻断本次执行」），不再让旧配置冒充当前绑定。同时「同一上游」的判定收敛到统一归一化：大小写、尾点、默认端口、路径、凭据变体不再被误当成不同上游，403 隔离与换路去重因此不再漏判（换将换个端口别名还撞同一家网关的空转消失了）。
- **建书链六连加固（t-20260921 双平台连败案）**：建书前置闸——开浏览器前按平台硬校验必填字段（番茄简介 50-500 字/书名/分类标签非空），过不了给人话提示，不再进浏览器烧一轮；兜底简介质量——markdown 洗成正文、不足 50 字从素材正文取（复用 _summary 净化），repair_existing 把简介纳入必填自愈；番茄建书流程「立即创建」改 submit 步骤尊重 auto_submit（与 UI「填好停人工提交」承诺一致），锚点切换 tries 8→12+弹层打开 settle（「主题」三连败）；七猫流程二级分类选完 settle 1s 再点「添加标签」（下拉关闭动画吞点击致标签弹层未开、标签组切换失败）；tags 组名点击重试 2→4 次、报错点名「弹层未打开」；url_any 断言失败时抓页面 toast/表单报错拼进错误信息——平台拒绝原因自己现形，不再误报「未登录或改版」
- **禅道失败回写三连修（#27783 案）**：修复任务失败若是 CodeBee 自身崩溃（运行错误为内部异常形态，如 0.1.63 的 `_TRANSIENT` NameError），不再转派端负责人——只评论+群通知人工，评论注明「工具自身异常，不代表修复结论」；只评论（未配负责人/内部崩溃）时改为先取当前指派人再回写——禅道编辑接口缺 assignedTo 会清空指派，杜绝「指派给(空)」记录；负责人未配置的失败认领备注点名未转派原因。
- **预估改为「开跑前点名基线 + 终态事后对账」，不设进队阻断**：同类型内部成本差 15~114 倍（实测 serial_novel 中位 12.1 万 / P90 185 万 tok），类型级预估切不开「正常长任务」与「跑飞」，所以开跑前不否决——阻断仍由 `budget.max_tokens_per_run` 在真实累计用量越线时判。改动三处：「全部」档不再把预估坍缩成 480 秒常数、快照优先取 P90 并落样本数（「近 90 天 · N 次同类」「同类仅 N 次样本，估得不准」「暂无同类历史，按流程基线估算」三档在 UI 上分得开（标准第 6 条：`samples < 10` 即薄样本，不得当实测承诺）），run 进终态那一刻把预估与真实 token/耗时写成一行对账记录（含比值、是否超 P90、快照来源），攒一个观察窗口再评估要不要收紧。
- **取消不再连累 KEY 与供应商了**：按「停止」取消一次任务，此前可能被记成真实故障——CLI 的取消错误串形如「取消；stderr/stdout: <被杀进程尾部>」，尾部恰好带 429、欠费或无效 KEY 字样时，会被整串文案匹配判定而给健康 KEY 记上 30 分钟冷却；同时该次取消还会被记成供应商失败，顺带作废 24 小时保质期内已 `passed` 的模型评测结论，后续任务因此绕开一家其实好好的上游。现在取消统一按结构化标记识别（`runner.attempt_cancelled`），三本惩罚账（KEY 冷却、供应商健康、评测作废）在取消时全部跳过；真实欠费与真实失败照旧记账，守卫同时钉住这半边，避免把豁免做成挡板。
- **首字延迟与吞吐入台账并进用量页**：内置直连的流式调用现在测 `first_token_ms`（请求发出 → 第一个带内容的增量，思考/正文/工具参数都算）与 `tokens_per_sec`（该次 output token ÷ 首末内容增量窗口），用量页新增「首字延迟」卡（均值 + P95 + 吞吐 + 样本数）。CLI 事件流没有逐 token 时刻、非流式回落同理，这类记录**不落字段**：缺字段=不可测，不参与任何均值，页面显示「暂无可测数据」而不是「首字 0 毫秒」。

## v0.1.65（2026-09-24）

- 修复「作品简介」生成出报告标题：非小说任务（如选题研究）拒绝生成作品信息——宁明确拒绝不产垃圾
- 简介净化：剥 markdown 标题符/列表符/加粗，压多空行
- 生成提示词硬约束：summary 必须是面向读者的推介文案

## v0.1.64（2026-09-24）

- 禅道看图排查自动降级链：Bug 截图进排查与修复链，编排模型不会看图就自动换将并落标记，不再甩人工处理
- 排查读图守卫：读不到图留人工判断，不盲判定论（#27754 案）
- runner 瞬态错误热回退修复

## v0.1.63（2026-09-24）

- 0.1.62 被并行发布占位（旧 HEAD，不含 runner 修复）；本版为完整版

## v0.1.62（2026-09-24）

### ✨ 新功能

- **任务计划活起来了**：`.codebee/task_plan.md` 从静态清单升级为随执行实时推进的活文档——每个子任务起跑标 `[>]`、成功翻 `[x]`、失败标 `[!]`（换将重试成功自动翻正）。任务崩溃、续跑、换将时打开计划文件一眼看到断点走到哪步；回写失败静默，绝不影响任务执行。
- **skill 装前扫描认得投毒特征了**：新增四组静态特征——下载即执行（`curl|sh` 管道、PowerShell `Invoke-Expression` 投放）、持久化驻留（crontab/计划任务/注册表自启动键，直接判高风险）、常见外传信道（pastebin/webhook.site/ngrok 隧道）、挖矿特征（stratum/xmrig）。中文叙述词零误报。
- **代码实现不再「顺手加固」了**：实现/修复提示词内置 HERO 反过度防御块——不加任务没要的哈希与校验和、不铺层层边界分支、不自造评分标准、不做顺手加固；只约束「提出什么」不约束「检查什么」，真实风险照找、写进总结由你决定。改动面更贴任务目标，少一轮「修过度工程」的返工。

### 📒 补记（更早已上线、此前未入版本节）

- 直连对话流式打印思考过程 + 网页成品实时预览页签 + 直连 `run_command` 真执行 + 连载分卷 + 多评审并发（同章两评审 7.2s→4.2s）+ 超时原样重烧闸门 + 429 限流与欠费同口径切备用 KEY + 工作汇报取禅道数 + 日志抽屉折叠 claude 思考心跳。

## v0.1.62（2026-09-24）

- 修复评审「命令行太长」：垫片检测此前只看首个参数（是 cmd.exe），漏判了真正的 .cmd 垫片——中等长度评审提示词直接撞 Windows 8191 上限。现扫全部启动参数
- 代码修复步超时 1200→2400 秒：真实排查型修复（读仓库/追调用链）不再被中途砍死

## v0.1.61（2026-09-24）

- 修复 mimo 评审器永远 Invalid API Key 的根因：mimo 不读任何环境变量 Key，唯一通道是配置内的 provider 块——现已自动注入（绑定即生效，Key/端点/模型一起落盘）。禅道双单 20+ 轮评审全灭实案
- 「完成·未达标」琥珀色胶囊：跑完但评审未通过的任务不再显示成「完成」，一眼可辨
- 同工作目录并行硬限制（后来者排队等待，60 秒自动重试）

## v0.1.60（2026-09-23）

- 同工作目录并行硬限制：两个任务共用同一工作目录时，后来者自动排队等待（每 60 秒重试，先来者完成自动放行）——不再只是告警，彻底杜绝任务计划/项目记忆/分支检出互相覆盖（禅道双单实案）
- 工作目录被占用时任务卡片显示「将于 HH:MM 自动续跑」，排队原因可见

## v0.1.59（2026-09-23）

- 评审器故障不再伪装「评审未通过」：评审器鉴权失效/崩溃时（实案：Invalid API Key 整晚空转修复轮），任务以「评审器故障（评审未执行）」明确收口，不再烧无意义的修复轮
- 同工作目录并行告警：两个任务共用同一工作目录时（任务计划/项目记忆/分支检出会互相覆盖），运行详情顶部显示⚠警告
- 后台页签让出 SSE 连接：多标签打开不再把前端接口全部挤到超时

## v0.1.58（2026-09-23）

- 修复「服务过一会儿就访问不到」：测试实例（自动化测试启动的服务）会通过启动清场误杀真实服务并鸠占鹊巢——现在测试实例拒绝拉起真实端口服务、启动清场自动跳过、禁止使用默认端口 8765
- 测试实例占用 8765 时启动将直接拒绝并提示改用独立端口

## v0.1.57（2026-09-23）

- 任务详情历史运行分页 + 加载提速（详情打开更快，支持「加载更早运行」）
- 修复：repeat-guard 判死后外层重试继续空转（stop=True 不生效，同输入最多多烧 5 次）——判死现在终态生效
- 直连对话时长统计修正；任务时限兜底行为补测试

## v0.1.56（2026-09-23）

- 鉴权错误不再触发换将：密钥失效类错误换哪个 CLI 都一样，直接报错省时间
- 超时任务提供「恢复」入口；排队任务状态文案更明确
- 驾驶舱轮询载荷瘦身（大屏常开不再吃带宽）；移动端任务详情操作按钮对齐

## v0.1.55（2026-09-23）

- 知识库可信度分级：引用核验借鉴——知识条目带 confidence 分级，low 直接丢弃，中高按置信度加权注入
- 编排执行稳定化 + 模型回退修复：调用链失败时的回退路径更稳
- 定时任务终态徽章补全：done/failed/cancelled 三分支全可见（失败必须一眼看到）
- 任务驾驶舱视觉改版；禅道 Bug 扫描过滤 + 去重防重录

## v0.1.54（2026-09-23）

- 任务驾驶舱大屏：/board.html 独立页 + /api/board 聚合端点——模型/厂商分布、实时思考流、排队滚动，中英双语
- 连载章节安全落盘：临时文件 + 原子改名，半截稿不再污染章节文件
- 日志抽屉一键复制全文（报错 JSON 不再手动圈选）
- 删除/归档乐观清场：SSE 半死时已删/已归档任务不再挂侧栏
- 卡死的模型调用快速失败转投，不再耗满超时
- run_end 钩子新增「完成后 AI 复查并自动提交推送」：AI 复查 diff 判 OK 才 commit+push
- 蜂巢卡显示当前使用模型 + 运行中思考过程行

## v0.1.53（2026-09-23）

- 换将救活两类「假终态」：上游内容拒答（"can't help with this" 一类 refusal）与真超时，现在都会换绑定链上下一个候选再试——此前拒答直接判死整轮任务（链上异上游候选从未被尝试），超时能不能换全凭输出尾段碰巧带 "timeout" 字样；真实案：夜间知识库整理任务被网关 gemini 拒答后整 run 白烧
- 同一慢上游连烧止损：链上候选连续两次相同超时即收手（换将=换壳不换命），最坏从 N×单步超时压到 2×；取消显式列为终态绝不降级
- 网关模型漂移可见：请求 A 模型被网关实际映射成 B 模型时（真实案：请求 deepseek-v4.1-flash 实服 gemini-3.8-flash），失败信息与步骤日志明确标注「请求 X，上游实服 Y」，排查看对病根

## v0.1.52（2026-09-22）

- 手动指定执行者优先级全面收口：用户点名的 CLI 压过一切自动选择——路由不换将、旁观不抢占、绑定不覆盖；「手动指定执行者」作为一等偏好随任务走
- 管理页执行者下拉可指定为对话执行者的真实 CLI（与新建任务两级菜单复用同一段逻辑）
- runner 静默挂死看门狗对「等结束才一次性输出」的 CLI 保留原语义，防误杀

## v0.1.51（2026-09-22）

- 智能路由：绑定链可用性细化为「有备胎」维度（解析出的调用链有第二条及以上的任务加分），静态能力基线不再把配额烧干的供应商顶在前面
- 内置智能体：用户显式指定实现者时全程尊重（不再被自动路由旁观抢占）；单次生成输出预算把推理模型的思考 token 也计入
- runner 错误识别降噪：审计日志错误行探针排除回显提示词里的普通词误判，尾段无错误信号时不强判失败
- 新增夜班竞品扫描脚本（scripts/borrow_scan_nightly.py）

## v0.1.50（2026-09-22）

- 今日体检修复：i18n 英文字典 22 个重复键清理——其中「 条/字/章」等 7 个值冲突属后值静默覆盖（如「第 N 章」曾被渲染成 "Chapter N chapters"），按使用现场裁定正确值，英文界面文案修复
- 测试适配：codex 配置同步两测试改「假 HOME 在临时目录下」放行形态，恢复验证真实写盘语义（防毒闸 011023c 引入的预存挂清零）
- 并行功能收尾一批：自动化运行偏好透传、内置智能体工作目录锁死、连载卷表显式化、市场已装包启停开关、思维链可视化、卷级规划要点、runner 缺件诊断、流式落盘节流、禅道间隔跟随默认
- 合并质量巡检：436 个本包导入绑定完整性通过（无漏提交模块）、pipeline 四组历史修复全部在位、架构检查无新增违规

## v0.1.49（2026-09-22）

### 🛠 问题修复

- 修复设置 →「钩子」卡片样式坍缩：下拉框漏写闭合标签导致命令框与操作行被吞进内部，整卡挤成一行。

## v0.1.48（2026-09-22）

### ✨ 新功能

- 新增「钩子」：在任务生命周期的关键点挂你自己的本机脚本——任务开始（输出注入任务上下文）、消息送达（输出注入提示词头）、运行结束（回写知识库等副作用）。命令从标准输入收 JSON，输出 `{"inject": "文本"}` 即注入；失败/超时静默跳过，绝不挡主流程。设置 →「钩子」里管理，支持逐条开关、超时与一键测试。

## v0.1.47（2026-09-22）

### ✨ 新功能

- 对话时间线支持复制与编辑重发：自己发的消息悬停出现「复制 / 编辑」，「编辑」把原文载入输入框，改完再发；智能体的回答悬停可「复制」（自动剥掉协议尾，复制到的就是看到的内容）。

## v0.1.45（2026-09-22）

### 🛠 问题修复

- 智能体读取超大文件（超 64KB 的日志/长文）不再只给开头：改为保留开头 44KB 与结尾 16KB、中段标注省略——报错和结论常在文件末尾，此前最关键的信息会被截掉；结尾处的中文也不会因字节截断而乱码。

## v0.1.44（2026-09-22）

### ✨ 新功能

- 经验库教训分型可视化：绿色「做法」徽章标记程序性记忆（从成功任务提炼的有效步骤），与普通「教训」一眼区分，悬停可见说明。

## v0.1.43（2026-09-22）

### ✨ 新功能

- 经验库新增「有效做法」类记忆：任务一次通过且评分高时，AI 会把「这次做对了什么」提炼成可复用步骤（如「做法：先列评分点再逐条应答」），下次同类任务自动注入照做——经验库从「只学教训」进化为教训+做法双类记忆。

## v0.1.42（2026-09-22）

### ✨ 新功能

- 新任务类型「标书编制」：上传招标文件，AI 按评分标准逐条应答起草投标文件——五大评审维度（应答完整性/合规符合度/方案针对性/评分点覆盖/商务清晰度），资质业绩只引真实材料（缺失标注待补，绝不虚构），应答先结论后细节，并全文自查废标风险项（密封/签章/格式/有效期）。

## v0.1.41（2026-09-22）

### 🛠 问题修复

- 断点续跑/重跑更省钱：同一任务再次生成大纲或执行计划时直接命中上次结果的精确缓存（1 小时内有效），不再重复花一次全量规划调用；大纲首试成功但格式不合规时的重试仍会重新生成（保「换一次样本」语义，不会拿同一份坏文本空转）。

## v0.1.40（2026-09-21）

### 🛠 问题修复

- 修复 npm 安装版微信直连脚本缺失：`tools/wx_reader.py` 未进打包清单，群列表刷新必报「No such file or directory」——现已随包分发。
- 「厂商/模型」选择器两步手选修复：点厂商只展开该厂商模型列表（不再自动带推荐，模型必须亲手点）；修复点厂商后菜单瞬间被收起、模型永远选不上的问题（真机定位：菜单重写 DOM 与「点外面收起」监听的竞态）；菜单改为向上弹出，不再被右侧栏遮挡。

### ✨ 新功能

- 对话输入条与新建任务同款三件套：编排模式 / 思考程度 / 厂商模型选择器直接嵌在输入条工具行，改动即写入任务参数（下一轮对话生效）；打开任务时自动回填当前值。
- 对话输入条的待发图片以缩略图展示（不再只是一串文件名），移除按钮悬浮右上角；气泡内图片缩略图不再压住输入框边框。

## v0.1.39（2026-09-21）

### 🛠 问题修复

- 修复长任务上下文撑爆后重试仍失败：压缩生成的会话摘要此前只写日志、没有真正替换发往模型的上下文；现在重试改用「压缩摘要 + 最近消息」重建请求并放弃膨胀的旧会话，超长连载/代码任务能真正续跑而不是再爆一次。

## v0.1.38（2026-09-21）

### ✨ 新功能

- 对话里发出的图片附件现在直接以 96×96 缩略图展示在气泡中（此前只有一串文件名），点击可放大查看；发图片时会提醒一次：若当前模型未开启「图」输入，模型将看不到这张图（可在模型接入页为模型打开图片能力）。

## v0.1.37（2026-09-21）

### ✨ 新功能

- 新建任务输入框焕新：编排模式、思考程度、对话模型收进输入框工具行右侧（与主流 AI 输入框一致的左右分区——左附件、右偏好+发送），不再是散落在框外的下拉；对话模型点开是「厂商 → 模型」两级菜单（选中打勾、底部「管理模型」直达），点厂商即带出该厂商的推荐模型，pill 以「厂商/模型」组合名显示，只对本次对话生效。
- 图片附件在输入框内以 88×88 缩略卡展示（此前首页按原图尺寸渲染，直接压住输入文字）。
- 「更多选项」里旧的对话模型下拉已移除（与工具行 pill 重复）。

## v0.1.36（2026-09-21）

### 🛠 问题修复

- 端口占用诊断的「关闭」现在真能关掉：服务类进程对温和关闭信号本就无反应，此前只发信号不看结果——现在温和无效会自动升级强制结束，并以「端口确实释放」为准，不再假报成功。
- 群摘要：选好 64 位 Python 后直接点「刷新群列表 / 立即扫描」不再误报「请先填路径」（表单会先静默保存）；「立即扫描」按钮文字不再重复；新建的群现在也会出现在监听列表（微信本地库还没同步时从最近会话补齐）。
- 桌面蜜蜂气泡的「点我细看」现在真的能点：点气泡直达群摘要页；网页已打开时也会跳到目标页，而不是只把旧窗口调到前面。
- 禅道：「测试并保存连接」不再卡住按钮几十秒（产品/账号清单改后台拉取，拉到后下拉自动就位）；操作状态面板显示具体操作名（如「拉取禅道产品」）；兼容更多老版禅道的模块树与产品清单返回形状，修「模块清单形状不认识」「产品只显示 ID」。
- 设置页「代码设置」恢复左标签右控件的横排布局（此前被章节评审卡的样式意外覆盖成整列竖排居中）。
- 修复页面繁忙时「上次使用的任务类型」偶尔没被恢复的时序问题。

### ✨ 界面

- 编排模式 / 思考程度 / 对话模型从「更多选项」提到输入框正下方，随开随用，不再折叠。

## v0.1.35（2026-09-21）

### ✨ 新功能

- 经验库越用越准：注入给任务的教训现在会按任务结局自动记「胜负」——任务过审说明这条经验真帮上忙（权重上升），失败说明没防住（权重下降）；同样相关时真正有用的经验优先注入，帮倒忙的逐渐被挤出。经验库随真实运行自我提纯，无需人工维护。

## v0.1.34（2026-09-21）

### 🛠 问题修复

- 手机端操作被「其他设备正在控制」挡住时，现在会弹出「接管并重试」确认框——点一下就接管控制权并自动重做刚才的操作，不再连点全是失败。
- 微信群列表拉取失败时把读取器的真实报错带出来（依赖缺失/解释器位数不对/微信未登录一眼可辨），不再只说「无有效输出」。
- 连载「章节评审」现在在侧栏进入的任务级详情也会显示，不再只有打开某一次运行时才能看到。
- 新建任务会一并记住上次的编排模式与思考程度；非法历史值会丢弃，不会污染表单。
- 修复扫描 Python 后尚未保存时被后台轮询冲回旧路径；现在点选或自动选中的候选都会保持到用户保存。

## v0.1.33（2026-09-21）

### ✨ 新功能

- 创建任务支持快速/自动/专家/手动四档编排，以及独立的自动/快速/标准/深度思考程度；所有预置类型按风险动态决定规划、评审、修复和换将步骤。
- 对话默认使用系统推荐，也可仅为本次对话指定厂商或模型；指定渠道不可用时明确失败，不修改全局 CLI 绑定。
- 创建前显示同类历史或流程基线 ETA/P90，运行中显示剩余时间与超出预估状态；满载时不排队，异常退出的运行自动收口为失败。

### 🛠 问题修复

- 修复 Windows GBK 控制台因特殊符号导致服务启动崩溃；Aider 启动前增加 Git 仓库可读性诊断，避免损坏仓库长时间卡住。
- 修复 Anthropic 兼容接口误收到 OpenAI `reasoning_effort` 字段；修复语言切换覆盖端口标题导致桌宠重复开窗；过滤 CLI 历史和评审中间文件，成果页只展示可交付产物。

## v0.1.32（2026-09-21）

### ✨ 新功能

- 连载任务详情新增「章节评审」卡片：每章的维度评分（低分标红）、达标状态、字数与修订轮数一目了然；每章按大纲逐项核对的判定结果（已完成/未完成/待核实）以红绿灰行直接展示——哪章哪个要点没写到一眼可查。

## v0.1.31（2026-09-21）

### ✨ 新功能

- 群摘要搬进正式页面：设置 →「微信群聊汇报」现在是完整页面——配置（监控文件夹/微信直连/Python 扫描与一键安装/监听群）和各群摘要列表一体呈现，不再挤在右下角悬浮小窗里。
- 桌面蜜蜂右键「群摘要」直达该页面（已打开的页面只聚焦不重复开）。

### 🛠 问题修复

- 修复微信直连「选不中 Python」：此前每分钟的自动刷新会把用户刚选好但还没保存的路径悄悄冲回旧值，现在改过路径后不再回填覆盖，保存成功后才更新。

## v0.1.30（2026-09-21）

### ✨ 新功能

- 微信直连配置更省事：新增「扫描」按钮自动找出本机全部 Python（标注位数与是否已装依赖，点选即回填）；选中未装依赖的 64 位解释器可「一键安装 wechatauto-replica」，不用再自己开 conda 敲命令。
- 桌面蜜蜂右键新增「群摘要」：直达群摘要面板（已打开的页面只聚焦不重复开）。
- 直连任务可选指定厂商与模型。

### 🛠 问题修复

- 修复没装 Pillow 的电脑上蜜蜂永远显示手绘丑蜜蜂：现在用系统自带能力直接显示新形象贴图（普通用户升级即变好看，无需装任何额外依赖）。
- 禅道自动化四个开关与扫描间隔紧凑排成一行。

## v0.1.29（2026-09-21）

### ✨ 新功能

- 升级后蜜蜂自动换新装：修复「升级后还是旧的丑蜜蜂」——服务重启后旧蜜蜂会自动让位，由新服务拉起带新形象的新蜜蜂（此前旧蜜蜂占着位置永不退场）。

### 🛠 问题修复

- 去掉蜜蜂底部的任务进度条（真机截图反馈）：窗口收紧贴着蜂体，画面更干净。

## v0.1.28（2026-09-21）

### 🛠 问题修复

- 「关于与更新」页的版本信息与更新说明改为左对齐阅读，不再整块居中（加载占位的居中样式误留在了正式内容上）。

## v0.1.27（2026-09-21）

### ✨ 新功能

- 连载任务防穿帮：新增「资源账本」——每章评审自动登记新出现的道具、伤情、承诺与伏笔，写下一章前自动注入提醒核对，角色不会上一章断臂下一章就挥剑。
- 连载评审逐项核对：每章按大纲要点逐项判定「已完成/未完成/待核实」并引用正文证据，铺垫不再被当成已完成。
- 新建任务记住你的偏好：上次选择的任务类型、轮数、阈值、章节数等下次自动预填（只记参数，不记内容）。

### 🛠 问题修复

- 代码任务提速：验证命令失败时先直接修复再评审（首轮不再白跑一次评审），连续失败两轮后才请评审参与诊断。

## v0.1.26（2026-09-21）

### ✨ 新功能

- 顶栏新增统一操作状态中心：创建、删除、绑定、插件等用户写操作会显示进行中、完成或失败状态，并保留时间与错误原因，慢请求不再像页面卡死。

### 🛠 问题修复

- 后台维护写请求、任务澄清探测和轮询请求默认静默，避免把后台动作误显示成用户操作；移动端状态弹层自适应屏幕并支持减少动画。

## v0.1.25（2026-09-21）

### ✨ 新功能

- 文档类任务更专业：工作汇报/商务邮件/技术方案/技术文档四类任务新增「分场合写作硬规则」——汇报先给结论不流水铺陈、邮件先答复再给背景且篇幅与轻重成正比、技术方案从问题开场并保留被否决的真实备选、文档标题写结果且验收标准可逐条勾选。起草与修订全链路生效。

### 🛠 问题修复

- 任务创建的澄清问答与删除响应链路优化（并行改进，一并入库）。

## v0.1.24（2026-09-21）

### ✨ 新功能

- 升级完成后自动重启：点「升级到新版」装好即自动重启生效，不再需要再手动点「重启服务生效」；页面几秒后自动恢复。有任务正在运行时不会自动重启，会提示稍后手动重启。
- 启动自动清场：启动时若服务端口被上次没退干净的旧实例占用，自动结束旧实例后启动，不再报「端口被占用」让用户手动排查。
- 运行详情页新增「归档任务」按钮：任务结束后可直接归档/取消归档，不用再找右键菜单。

### 🛠 问题修复

- 修复任务附件偶尔被智能体无视：附件说明从「请参考」改为硬约束（动手前必须先读附件，不读视为未完成；读不了要明确告知缺什么）。截图/文档类附件在直接执行、代码等各类任务中都会被认真读取。
- 修复桌面蜜蜂「关不掉」：右键「关闭桌宠」现在真正关闭，不再被自动复活。
- 修复桌面蜜蜂「拖不动」：拖动功能此前在窗口上绑定了不存在的方法（异常被静默吞掉），现已修复并做了节流优化，拖动跟手流畅。
- AI 味检测新增叙事架构层：顿悟说教/情绪身体化/成长式收束等措辞改写不掉的架构级指纹，随评审下发情节结构追问。
- 经验库预算纪律：通配技能包单包限额、项目教训保底注入——再多的通配包也挤不掉你沉淀的教训。

## v0.1.23（2026-09-21）

- 端口占用诊断（借鉴 leftopen）：启动时端口被占自动指认占用者（PID/进程/所属项目，不再让用户手跑 netstat+tasklist）；设置页新增「端口占用」面板——扫描本机全部监听端口（仅本机/本服务徽章、项目归属列），可对非自身进程发送温和关闭信号（SIGTERM 语义，关前重验 PID，系统进程与 CodeBee 服务自身拒绝关闭）
- 经验库预算纪律：通配（wildcard）技能包单包注入限额 2400 字、项目教训保底注入——此前 39 个通配包全文注入会吃光 9000 字上限，把本机沉淀的评审教训整段挤掉；定向命中的规范包（番茄/七猫签约标准等）不受单包限额
- AI 味检测新增叙事架构层（借鉴 sepia/StoryScope 研究：AI 小说 93.2% 靠叙事架构特征检出，人工改措辞后几乎不降）：确定性统计顿悟说教/情绪身体化/成长式收束（只扫结尾 600 字）三类架构级指纹，命中即随评审下发情节结构追问
- 任务画像与调度统一：运行详情新增「任务画像与调度」审计——任务画像与实际选路来自同一次编译结果，难度/能力需求/降级链一目了然

## v0.1.22（2026-09-21）

- README 功能导览大扩容：新增知识库、自动化、帮助中心、作品信息图文介绍
- 近期亮点回顾：语音识别词表纠偏、调研报告「结论先行」硬约束、任务默认立即启动

## v0.1.21（2026-09-21）

- 任务默认直接启动，不再进入容量等待队列；达到并发保护上限立即失败并明确提示稍后重试，历史 queued 记录启动时自动接管或收口。
- 修复取消竞态：编排、管理、自升级、禅道修复在取消后不会复活；自动续跑重启恢复不再受隐藏数量上限影响，并显示明确的续跑时间。
- 定时任务、重试、续写、答疑和禅道修复统一使用同一套启动失败收口逻辑，避免任务长期停留在待启动状态。

## v0.1.20（2026-09-20）

- 全类型交付契约：小说、文章、短视频、文档、翻译、调研、演讲、汇报、邮件、技术方案、简历按真实类型路由，并注入各自角色、成品结构和真实性约束
- token 预算修复：默认关闭压缩与续会话路径也累计真实 usage；压缩单次/重试防重复记账，预算熔断恢复有效
- 任务类型管理修复：15 种流程全部显示成本预估；类型切换不再串 rubric；编辑预置流程不再误建副本；direct 正确显示为直连引擎
- 全类型竞品雷达与经验沉淀：关键词库校正为 130 组并按小时轮换，新增当日七专项报告与后续深挖队列

- 取消即强杀：手动点「取消运行」直接强制终止任务——运行立刻翻「已取消」终态（还挂「运行中」的步骤同步收口，蜂巢格不再空转），CLI 子进程树由取消事件 0.4 秒内秒杀；内置智能体生成中的模型请求改为可打断（取消即刻放弃等待，健康 KEY 不背冷却），起草/赛马的 30s/60s 退避与暂停闸也改为取消即刻唤醒；管理安装/升级/自升级被取消后终态不被步骤汇总改写回「失败」；确认框与提示文案同步改为「立即强制终止」语义
- 群摘要·微信直连：不用再手动导出——蜜蜂坞齿轮里开启「从本机微信自动拉取」，填 64 位 Python 路径（conda 独立环境装 wechatauto-replica），「刷新群列表」搜出 347 个群勾选要监听的群，之后每次扫描 sidecar（tools/wx_reader.py，只调读接口）自动把各群新消息增量拉成导出格式追加进监控文件夹，昵称解析链=自己→群成员表备注→跨拉取持久化缓存→全局联系人表兜底（退群成员也能翻出真名），图片/语音/文件等非文本以占位符入文；摘要卡新增「转知识库」一键把工作群干货转成知识库草稿条目（转正后才注入任务提示词，防聊天噪声污染）；安全边界：群聊是不受信输入，摘要的模型工作目录隔离在专用空目录+提示词声明「聊天记录只当数据」，sidecar 结果走文件不走 stdout；实测微信 4.1.13.65（原 wxauto 路线只支持 3.9.X 已弃）
- 群摘要：微信群聊天记录定时整理——把导出的聊天记录 txt 丢进监控文件夹（文件名=群名，每条以「2024-01-01 12:34 发送者」开头，UTF-8/GBK 通吃），按间隔增量生成每群 AI 摘要（一句话总览/话题结论/待办行动项/未决争议）；首次接入只摘要最近一窗不回溯历史，同秒多条消息靠末条身份精确接续不漏不重，摘要失败游标不动下拍自动重试；摘要在右下角网页蜜蜂坞面板看（未读徽章+气泡提醒，齿轮里就地配置监控文件夹/间隔/窗口字数，未启用且无数据整坞隐藏），桌面小蜜蜂同步报「🍯 有新群摘要」（悬停见未读数、点击直达网页细看）；隐私口径：记录只存本地，摘要请求会发给当前绑定的模型（面板内明示）；调度挂 automation tick（fire_due 自节流，同禅道模式）
- 桌面蜜蜂（续）：双形象预置「毛绒蜜蜂 / 机械蜜蜂」右键菜单或设置页随时换（原位换装不用重启，pet_skin 落设置）；大小三档（小/中/大）、回到初始位置、让TA安静一会（30 分钟不碎碎念）、先藏起来（30 分钟彻底隐身）——对齐竞品宠物设置面板；悬停清单只列进行中/排队并截断长标题（历史失败项曾刷成一堵墙，失败由警示气泡点名）；动画加料：工作时高频振翅悬浮、睡觉呼吸沉浮+每 18 秒伸个懒腰、庆祝变成跳跳摇摆舞、冒气泡蹦一下、待机偶尔冒一句蜂言蜂语；拖动换 Tk 内置 scan_dragto（C 级跟手，原来逐事件 geometry 卡成 PPT）且拖动中冻结姿态；全机唯一蜜蜂——单实例锁从数据目录改到系统临时目录，多服务实例/开发仓+正式版并存也只养一只（多只蜜蜂真机踩坑）
- 桌面蜜蜂：CodeBee 桌宠（参考 Codex Pets 产品形态）——tkinter 零依赖自绘小蜜蜂常驻桌面右下角，实时反映蜂群状态：空闲打盹（Zzz）、任务运行振翅悬停（徽章显示数量+步骤进度条）、全部完工翻滚庆祝（气泡「N 个任务完工！」）、失败/取消警示抖动（气泡点名问题任务）；鼠标悬停弹出「蜂群动态」任务清单（进行中/排队/失败实时刷新，含当前步骤与进度）；左键打开 CodeBee、右键菜单（显示模式/语言/关闭）、按住拖动且位置记忆；跑在独立小进程（pick_dialog 同款进程隔离），服务关闭约 1 分钟内自离不留孤儿；设置页「皮肤」子页新增开关与「常驻 / 仅任务运行时」显示模式（默认开、常驻）；设置是唯一真源——关闭后蜜蜂下个轮询周期自离、看护线程不复活；单实例心跳锁防双开

## v0.1.19（2026-09-20）

- 知识库零负担化：条目默认直接转正参与注入（提炼提示词自带质量约束兜底），历史草稿启动时一次性转正
- 发布管理器自愈：启动收尸把等扫码/忙状态的死线程改判 error；旧版冤判「等待登录超时」的记录后台复核，登录态还在的直接翻回已连接
- 禅道地址自适应：REST 根路径 404 时自动探测 /zentao 子路径部署并缓存，另提供产品/账号清单接口供前端下拉
- 新增竞品调研流程：抓取七猫+番茄排行榜公开数据，AI 提炼跨平台热门题材/人设/差异化切入点

## v0.1.18（2026-09-20）

- 知识库：运行产出自动整理成可复用知识——编排者从产出材料（调研报告/文档）提炼知识条目（草稿态），人工「转正」后按任务类型+相关性自动注入同类任务的规划与评审提示词；与经验库分工（教训记「别这么做」，知识记「已知是这样」）；设置页独立「知识库」子页，搜索/状态/标签过滤+手动新建（即个人知识管理库）；事实带账龄，as_of 超 90 天注入时自动标注「可能过期」
- 封面生成：智谱 CogView 接线（CODEBEE_IMAGE_WATERMARK=1 才保留供应商水印）；封面落运行目录，专用通道内嵌缩略图
- 七猫发布纠偏：目标读者/分类旧数据不自洽时按官方目录反查，不再死在流程中段
- 队列补队治理：CLI 安装/升级等管理操作也纳入启动补队与巡检（selfupgrade 除外），排队不再无人接案

## v0.1.17（2026-09-20）

- 任务宪章：工作目录 .codebee/constitution.md 一次定义质量原则（代码规范/文风/测试要求），每次运行自动注入所有智能体——注入体系五层就位（宪章>圣经>模块库>经验>记忆）
- 代码评审证据锚定：每个问题必须带「文件名:行号」+一行关键代码引用，无证据不报（借鉴 pr-af）
- 调研报告证据链：同结论双源交叉验证、来源行内标明、事实与观点分离、结论先行+风险局限结构（借鉴 gpt-researcher）
- 评审维度写前自查：起草提示词亮出评审打分维度，作者提前对齐重点
- 圣经连续性锁：跨章设定原样沿用不问义改写，每章用词一致读者不出戏（借鉴 drama-skills）

## v0.1.16（2026-09-19）

- 帮助中心：首启欢迎引导升级为「左目录 + 右内容」多章帮助中心，八章全就绪——快速上手、模型接入与绑定（多 Key 轮换 / 协议徽章解读 / 报错速查 / 免费档起步）、功能一览、连载创作流程、发布上架、代码任务与版本、自动化与技能市场、常见问题（9 条踩坑 FAQ）。首启仍只自动弹一次落在「快速上手」；手动打开（设置导航/关于页/Ctrl+K/**F1**）徽章与标题显示「帮助中心」，原三处「使用引导」入口统一更名
- 就地帮助问号：模型接入、CLI 绑定、智能体目录、自动化、插件市场、禅道、用量统计、经验库八个设置页头 + Git 工作台/代码版本隔离/蜂巢工作台/作品信息四处工作台头部共十二处新增小「?」，点击直达帮助中心对应章节；供应商空状态与「还没有装好的 CLI」空状态附「看帮助」链接
- 帮助中心新增「代码任务与版本」章（任务分支隔离 / 版本裁决合并与丢弃 / 蜂巢与步骤阅读指引），「自动化与技能市场」章补禅道 Bug 自动修复小节

## v0.1.15（2026-09-19）

- 禅道集成：定时扫描激活 Bug → 自动建修复任务 → 跑完自动合并 + resolve(fixed) + 报告回写 + 群通知（REST v1，SSRF 守卫；设置页「禅道」子页）
- 扫榜双源聚合：七猫+番茄 Web 版排行榜合并分析，跨平台重合题材单独标记（信号更强）
- 可分享报告页：运行报告导出为自包含单文件 HTML，发给任何人浏览器直接打开
- 出站 HTTPS 加固：统一证书校验上下文，修复 macOS python.org 发行版缺 CA 导致的全部外联失败（校验只增不减）
- 引擎调参卡：设置页可直接调压缩阈值/尾部保留/goal 轮数等引擎参数（schema 驱动，revision CAS 防并发覆盖）
- 发布浏览器 profile 迁至 ~/.codebee（登录态自动迁移），不再占用仓库目录与拖慢扫描
- 连载发布真机校准续：番茄编辑器结构校准、发章导航超时放宽、真实鼠标事件提交（合成点击被平台检测）

## v0.1.14（2026-09-19）

- 新任务类型「扫榜选材」：抓取七猫排行榜公开数据，AI 提炼热门题材 Top3/高频人设套路/差异化切入建议，直出选题洞察报告
- 运行结果群推送：设置群机器人 webhook（钉钉/飞书/企微自动适配），任务跑完自动汇报状态与评分

## v0.1.13（2026-09-19）

- 封面图生成：连载任务建书面板新增「生成封面」——调编排者供应商图像 API（cogview 候选，竖版优先）产出 cover.png 到运行目录，SSRF 边界校验 + curl 子进程落盘（Python 不经手图像字节）
- 需求拷问采访态：目标描述过短时先问 1-3 个澄清问题（选项芯片点选），创建更准
- 代码任务 Best-of-N：单路实现失败后多候选 worktree 隔离赛马择优
- 计划落盘任务档案：.codebee/ 三件套（spec.md 意图 / task_plan.md 计划 / evidence.md 验证证据）
- 步骤卡「实际派发模型」徽章 + 类型菜单成本预估行

## v0.1.12（2026-09-19）

- 代码任务计划落盘：每次运行把编排计划写进工作目录 .codebee/task_plan.md，与任务规格（spec.md）、验证证据（evidence.md）同居「任务档案」，断点续跑/复盘全程可查
- 多子任务防漂移：实现子任务提示词自动带上「第 i/N 项 + 已完成清单」，长任务链不再重做已完成项
- 连载剧情模块库：plot-modules.md（拆文沉淀的桥段/爽点素材）每章自动注入
- 连载赛马败者精华：落选候选稿的优点自动并入首轮修订意见

## v0.1.11（2026-09-18）

- **连载发布闭环（主轨）**：番茄/七猫作家后台 CDP 直操通道（连接扫码/建书/发章/表单探测/截图存证）+ 详情页出版台面板；发布台账幂等（章号去重）+ 三道护栏（每日上限/连续失败暂停/人工确认闸——默认填好表单停，提交权留给用户）
- **定时自动发布（P2.5）**：任务级 task.auto_publish 到点触发批量发布；直发模式需 flows 校准文件解锁；批量发布按章号顺序、失败即停
- 代码任务 Best-of-N：单路失败后 worktree 隔离赛马兜底，验证通过+改动最小者胜（借鉴 orca）
- 需求拷问采访态：goal 模糊时先问 1-3 个澄清问题再开跑（借鉴 grill-me）
- 任务队列双病根根治：job 蒸发看门狗自愈 + 排队回填；同因连撞不再重复退避付费；失败退避等待在运行行/运行详情/执行结果/运行记录处显示「将于 HH:MM 自动续跑」，不再笼统显示排队中
- 运行前配置同步防线：每次真实派发自动把绑定链首落进 CLI 自家配置——绑定换供应商后不再撞上旧 CLI 配置空跑（opencode 钉死旧端点案）
- 评审新增「AI 味」确定性检测：译制腔/套话密度统计随评审下发参考线（借鉴 oh-story，命中才提示、评审官结合上下文判断）
- 任务规格文件化：任何任务在工作目录留 .codebee/spec.md（目标/参数/验证命令），随任务分支版本化、复盘续跑可见
- Git 变更视图豁免 .codebee/ 元数据目录，面板只看任务产出
- 报告区失败/空报告/无报告分态可见提示，不再静默留白
- 新增类型菜单成本预估行、步骤卡「实际派发模型」徽章、建 PR 自定义标题（承接 v0.1.10 未列项）

## v0.1.10（2026-09-18）

- Git 工作台「建 PR」支持自定义标题：提交信息框有内容时自动兼作 PR 标题（一处输入两用）
- Best-of-N 败者精华并入修订：终审从落选候选稿提炼的优点随评审意见一并喂给作者
- 报告与对话的围栏代码块统一渲染；竞品调研知识库（docs/borrow-log/knowledge.md）建立并沉淀 40+ 项目

## v0.1.9（2026-09-18）

- 类型菜单项显示成本预估：每个任务类型一行「≈N tokens · N 次」（近 90 天同类台账中位数，无样本自动隐藏）
- 详情页步骤卡新增「实际派发模型」徽章：换将/链回退时一眼看清每步实际打了哪个模型
- 直连对话新增「建议追问」芯片：助手回答尾部最多 3 条可点建议，点击回填输入框（借鉴 freebuff）
- 新建任务支持 Best-of-N 候选并发（评审类单稿任务）：多路不同写法策略并行起草，终审择优并吸收落选稿优点
- 换将超窗预检：切到小上下文模型前先按其容量预压缩，避免第一个请求被拒
- 外部触发 Webhook：POST /api/hooks/run（X-CodeBee-Token 令牌鉴权，未配置仅限本机）
- Git 工作台新增「建 PR」：推送当前分支并调 gh 创建 PR（base 自动识别 main/master）

## v0.1.8（2026-09-18）

- README 全面重写：功能导览 + 9 张实机截图，补齐蜂巢/经验库/插件市场/自动化/对话模式等章节
- 修复：孤儿 CLI 清扫的 PowerShell 查询在部分机器上拖慢启动 60 秒——改为后台线程执行，不再挡服务就绪
- 连载韧性加强：死链步骤自动补位同池活链 CLI；网关限流退避窗口可视化（排队可开详情）；启动时补队收尾
- 连载答疑：追话 QA 单步答疑不重跑；评审解析三道网（JSON 内嵌引号/正文提分兜底）
- 目录页管理操作四道防线：假新版本缓存作废复检、同条目去重闸、EBUSY/EPERM 人话提示、aider 迁移 uv tool
- 评审全挂不再误判「未达标」：补位评审 + 判失败；超长提示词落临时文件；打磨重评不覆盖好分
- 新增：欢迎引导向导、手工添加模型、wire 协议探测、详情页布局治理

## v0.1.7（2026-09-18）

- 移除「绑定链·xxx 绑定链失效」红色告警胶囊：从没配置过绑定链的 CLI 不再被误伤（0.1.6 真实装机案例，更新后人人都见红胶囊），回落 CLI 本机默认照跑
- 明确配置过绑定链但链上供应商全部失效时，仍会在该步骤明确报错不静默降级（防静默烧配额的本意保留），只是不再有全局胶囊
- 老版本残留的绑定链告警状态在升级重启后自动清除
- 修复：Windows 上一键升级秒挂（npm EBUSY）——npm 换版本要整体改名包目录，升级命令与就地重启的工作目录却落在包内；现统一钉到包外
- 修复：升级失败后「关于与更新」页永远停在「正在升级…」（升级状态轮询读错返回结构），现在成功/失败都会正常提示

## v0.1.6（2026-09-18）

- 连载韧性：网关限流/欠费时章稿失败先退避重试同作者（保住连载风格一致），重试穷尽才判死整 run；自动续跑改为延迟 5 分钟入队、上限提升到 3 次
- 修复：任务分支从游离 HEAD 检出时「切回原分支」原地空转的假成功；游离基线的合并现在显式拒绝并说明
- kimi 接入：僵尸 kimi 进程自动检测清理；CodeBee 托管配置按 TOML 语义渲染
- 修复：generic 类 CLI 经 npm .cmd 垫片时提示词被 cmd 重解析截烂（改 node 直启）
- 界面：新增蜜蜂图标，蜂巢工作台与皮肤视觉精修

## v0.1.5（2026-09-16）

- 更新提示升级：发现新版本时直接展示「新版本更新内容」，升级后首次打开自动弹一次「本次更新内容」
- 新增 CHANGELOG.md 并随包分发

## v0.1.4（2026-09-16）

- 修复：部分安全软件环境下启动卡在「正在启动任务队列」——worker 线程改为首次有任务时再创建，启动期不再创建任何线程
- 启动任一阶段卡超过 20 秒会自动打印各线程卡点堆栈（就绪后自动关闭），截图即可定位问题

## v0.1.3（2026-09-16）

- 启动过程逐步骤显示进度与耗时，慢硬盘/杀软冷启动不再像卡死
- 服务就绪后明确提示「不要关闭本窗口」

## v0.1.2（2026-09-16）

- 源码仓库地址改为 github.com/Vercel-By-WXP/CodeBee（npm 元数据同步）

## v0.1.1（2026-09-16）

- 品牌 CodeBee 化：任务分支前缀 `codebee/<任务ID>`（老 `tutti/` 分支仍可合并/丢弃），git 提交身份改为 CodeBee
- 数据目录：新装落 `%APPDATA%\CodeBee`（macOS/Linux `~/.codebee`），老 Tutti 目录自动沿用零迁移
- 启动横幅与失败排障指引（多 Python 机器可用 `TUTTI_PYTHON` 切换）
- README 重写快速开始：npm 安装为一等路径

## v0.1.0（2026-09-16）

- npm 首发完成：`npm install -g codebee` 一条命令即用，自带自更新
- 多智能体编排台全家桶：目标拆解/智能路由/执行/验证/跨厂商评审/连载/经验库/插件市场
